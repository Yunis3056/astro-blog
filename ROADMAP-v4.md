# Yunis 的博客 · v4.0 路线图

> v4.0 目标：从“可发布博客”升级为“可长期沉淀、可导航、可讨论、可度量的中文知识系统”。

## 北极星

读者进入站点后，不只看到按时间排列的文章，而是能沿着专题、系列、搜索和相关文章继续探索。

判断标准：

- 内容能按专题和系列组织，旧文章、笔记、项目可以互相连接。
- 搜索能按类型、专题、标签和日期缩小范围。
- 读者可以讨论，维护者可以看到增长信号。
- 所有能力仍保持静态站主体，外部服务可配置关闭。

## M1 · 内容模型

- [x] 新增 `topics` 和 `series` 内容集合。
- [x] blog / notes / projects 增加专题、精选等字段。
- [x] blog / notes 增加系列和 canonical 字段。
- [x] 新增内容验证脚本，检查引用和系列顺序。

## M2 · 信息架构

- [x] 新增专题索引、专题详情、系列索引、系列详情。
- [x] 首页改为知识入口。
- [x] 文章页加入专题、系列进度和相关文章。
- [x] Header 提升专题 / 系列入口，保留标签。

## M3 · 搜索与增长

- [x] Pagefind 输出 type / topic / tag / date 元数据。
- [x] 搜索弹窗支持筛选和日期排序。
- [x] Giscus、Plausible 作为可选增长能力接入。
- [x] 未配置外部服务时使用静态降级入口。

## M4 · 发布基线

- [x] CI 运行内容验证、类型检查、构建、产物验证和链接验证。
- [x] README、CHANGELOG、内容模型、写作流程、部署和质量门禁同步 4.0。

## 质量门槛

- `npm.cmd run check` 通过。
- `npm.cmd run verify:content` 通过。
- `npm.cmd run build` 通过。
- `npm.cmd run verify:dist` 通过。
- `npm.cmd run verify:links` 通过。
- `/topics/`、`/series/` 可访问。
- 搜索可以按类型、专题、标签和日期排序。
