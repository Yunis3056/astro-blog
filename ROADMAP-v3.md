# Yunis 的博客 · v3.0 路线图

> v2.0：让站点能长期写、能被检索、具备完整博客基础设施。
> v3.0 目标：把博客升级为可发布、可维护、可度量的长期内容系统。

---

## 北极星

每次写作、预览、发布和维护都应该有清晰入口。

判断标准：

- 写作者知道该写什么字段、走什么流程、发布前检查什么。
- 读者能通过标签、归档、搜索、系列和相关推荐继续阅读。
- 维护者能通过文档、CI 和质量门禁判断站点是否处于可发布状态。

---

## 版本策略

v3.0 先做文档和流程基线，再做功能扩展。每个里程碑都应该可以单独提交、单独验证。

### 在做

- 内容模型体系化：系列、精选、项目、笔记、外部链接等字段逐步标准化。
- 发布流程稳定化：本地检查、构建、搜索索引、OG、RSS、链接检查进入固定门禁。
- 阅读体验增强：相关文章、系列导航、404、搜索元信息。
- 运维文档化：依赖升级、字体、OG、Pagefind、部署故障有记录。

### 暂不做

- 服务端动态功能。
- CMS / 后台编辑器。
- 必须登录的互动能力。
- 多语言全量改造。英文内容可以作为内容模型预留，不作为 v3.0 阻塞项。

---

## M0 · 发布基线

目标：把 v2 已完成能力固化为 v3 起点。

- [x] 建立 `ROADMAP-v3.md`
- [x] 建立 `CHANGELOG.md`
- [x] 更新 `README.md`
- [x] 建立 `docs/` 文档入口
- [ ] 为 v2.0 打 tag 或保留发布说明
- [ ] 如需归档，将 `ROADMAP-v2.md` 移动到 `docs/archive/`

验收：

- `README.md` 描述和当前功能一致。
- `CHANGELOG.md` 能看出 v2 已完成能力和 v3 计划。
- 新贡献者可以只读文档完成本地安装、写文章、构建和预览。

---

## M1 · 内容模型

目标：让内容字段足够稳定，后续页面和自动化不靠临时约定。

P0：

- [ ] 为 blog frontmatter 增加 `featured`、`series`、`canonicalURL`、`coverAlt` 的设计决策。
- [ ] 明确 `draft`、`tags`、`updatedDate` 的使用规则。
- [ ] 新建或完善文章模板，让 `npm.cmd run new` 生成符合规范的草稿。
- [ ] 给项目页和笔记页确定是否进入 Astro content collections。

P1：

- [ ] 支持系列文章导航。
- [ ] 首页可展示精选文章。
- [ ] 标签规范化，避免同义标签分裂。

验收：

- `docs/content-model.md` 和 `src/content.config.ts` 一致。
- 新文章从创建到发布不需要手动猜字段。
- 草稿内容不会进入列表、RSS、标签页、OG 和搜索索引。

---

## M2 · 发布流程

目标：减少“本地没问题，发布后坏了”的风险。

P0：

- [ ] GitHub Actions：安装依赖、`npm.cmd run check`、`npm.cmd run build`。
- [ ] 链接检查：内部链接、外部链接、图片路径。
- [ ] 构建产物抽查：Pagefind 索引、OG 图片、RSS、Sitemap。
- [ ] 明确部署目标和站点 URL。

P1：

- [ ] Lighthouse CI 或手动 Lighthouse 报告模板。
- [ ] 发布 checklist 文档化。
- [ ] 依赖升级流程文档化。

验收：

- 主分支每次变更都有自动构建结果。
- 发布前检查项写在 `docs/quality-gates.md`，不是散落在聊天记录里。
- `site` 配置、RSS 链接和 OG 链接在生产环境一致。

---

## M3 · 阅读体验

目标：让读者更容易继续读下去。

P0：

- [ ] 404 页面。
- [ ] 相关文章模块。
- [ ] 系列文章入口。
- [ ] 搜索结果元信息优化，至少包含标题、摘要、日期。

P1：

- [ ] 文章末尾 GitHub Discussions 链接。
- [ ] 标签页筛选或排序增强。
- [ ] 归档页按年份快速跳转。

验收：

- 移动端阅读路径完整，导航不溢出。
- 搜索、主题切换、目录和 ClientRouter 切页后仍可用。
- 所有增强都保持静态站点特性。

---

## M4 · 可选增强

这些能力不阻塞 v3.0 发布，适合在主流程稳定后再做。

- 隐私友好统计：Umami 或 Plausible。
- Webmention / 反向链接。
- 英文内容或双语页面。
- 项目详情页。
- 笔记页改为 micro-blog 时间流。

---

## 质量门槛

v3.0 发布前至少满足：

- `npm.cmd run check` 通过。
- `npm.cmd run build` 通过。
- Pagefind 只索引公开内容。
- RSS、Sitemap 和 OG 不包含草稿。
- 主题切换在 ClientRouter 切页后有效。
- 搜索快捷键、输入、键盘导航有效。
- `/blog/`、文章页、标签页、归档页在小屏不溢出。
- README、CHANGELOG、docs 与真实功能一致。

---

## 风险与取舍

- 内容模型过早复杂化会拖慢写作。新增字段必须有页面或自动化消费方。
- 中文字体资源很大，OG 字体继续优先使用本地可提交字体，不依赖构建时联网。
- Pagefind 在 `postbuild` 执行，任何部署平台都必须保留完整构建流程。
- ClientRouter 会替换 DOM，所有客户端脚本都要能在 `astro:page-load` 后重新绑定当前 DOM。
